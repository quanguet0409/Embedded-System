#include "leds.h"
#include <MKL46Z4.h>

void init_leds(void)
{
    // Initialize the LEDs
}

void update_leds(uint8_t active)
{
    if (active)
    {
        // Turn on green LED, blink at 1Hz
    }
    else
    {
        // Turn on red LED
    }
}
